
public class Exercise3_1 {
	// 練習問題3-1
	// 日本語で記述された条件式をJavaで記述
	public static void main(String[] args) {
		// 変数は適当に宣言
		// ①変数weightの値が60に等しい。
		int weight = 0;
		if(weight == 60) {}

		// ②変数age1とage2の合計を2倍したものが60を超えている。
		int age1 = 0;
		int age2 = 0;
		if(age1 + age2 > 60) {}

		// ③変数ageが基数である。
		int age = 0;
		if(age % 2 == 1) {}

		// ④変数nameの中身の文字列が「湊」と等しい。
		String name = "";
		if(name.equals("湊")) {}
	}

}
